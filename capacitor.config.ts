import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.omnituner.app',
  appName: 'OmniTuner',
  webDir: 'dist/tuner-app/browser',
};

export default config;
